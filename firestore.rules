
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    function getUserData() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
    }

    // USERS
    match /users/{userId} {
      allow read, write: if isOwner(userId);
      // PERMET DE CRÉER LE DOCUMENT USER AUTOMATIQUEMENT
      allow create: if isSignedIn() && request.auth.uid == userId;
    }

    // WODs PERSONNELS
    match /users/{userId}/wods/{wodId} {
      allow read, write: if isSignedIn() && isOwner(userId);
    }

    // COMMUNITY WODs
    match /communityWods/{wodId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;
      allow delete: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow update: if isSignedIn();

      match /reactors/{reactorId} {
        allow read: if isSignedIn();
        allow create: if isSignedIn() && isOwner(reactorId)
                       && (getUserData().premium == true || getUserData().dailyReactions < 5);
        allow delete: if isSignedIn() && isOwner(reactorId);
      }
    }
  }
}

    